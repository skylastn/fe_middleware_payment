{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Dev (Development .env)",
      "type": "node-terminal",
      "request": "launch",
      "command": "bun run dev",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Dev (Production .env)",
      "type": "node-terminal",
      "request": "launch",
      "command": "bun run dev:prod",
      "cwd": "${workspaceFolder}"
    }
  ]
}
